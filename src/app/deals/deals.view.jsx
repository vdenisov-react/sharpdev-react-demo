import React from 'react';
import './deals.styles.scss';

export default ({ ctrl }) => (
    // ##################################################

    <span className='page-title' style={{ color: 'green' }}>
        {ctrl.pageTitle}
    </span>

    // ##################################################
);
