import React from 'react';
import './deals.styles.scss';

export default ({ ctrl }) => (
    // ##################################################

    <app-deals>
        <span className="page-title">{ctrl.pageTitle}</span>
    </app-deals>

    // ##################################################
);
