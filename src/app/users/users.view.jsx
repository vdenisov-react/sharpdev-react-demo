import React from 'react';
import './users.styles.scss';

export default ({ ctrl }) => (
    // ##################################################

    <app-users>
        <span className="page-title">{ctrl.pageTitle}</span>
    </app-users>

    // ##################################################
);
