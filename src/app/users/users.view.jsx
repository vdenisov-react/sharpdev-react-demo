import React from 'react';
import './users.styles.scss';

export default ({ ctrl }) => (
    // ##################################################

    <span className='page-title' style={{ color: 'blue' }}>
        {ctrl.pageTitle}
    </span>

    // ##################################################
);
