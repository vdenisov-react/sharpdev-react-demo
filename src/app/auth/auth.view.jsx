import React from 'react';
import './auth.styles.scss';

export default ({ ctrl }) => (
    // ##################################################

    <app-auth>
        <span className="page-title">{ctrl.pageTitle}</span>
    </app-auth>

    // ##################################################
);
