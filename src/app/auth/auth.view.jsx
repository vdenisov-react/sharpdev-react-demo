import React from 'react';
import './auth.styles.css';

export default ({ ctrl }) => (
    // ##################################################

    <span className="page-title" style={{ color: 'red' }}>
        {ctrl.pageTitle}
    </span>

    // ##################################################
);
