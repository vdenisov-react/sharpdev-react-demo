import React from 'react';
import './deals.styles.css';

export default ({ ctrl }) => (
    // ##################################################

    <span className="page-title" style={{ color: 'green' }}>
        {ctrl.pageTitle}
    </span>

    // ##################################################
);
