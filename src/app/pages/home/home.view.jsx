import React from 'react';
import './home.styles.css';

export default ({ ctrl }) => (
    // ##################################################

    <span className="page-title">{ctrl.pageTitle}</span>

    // ##################################################
);
