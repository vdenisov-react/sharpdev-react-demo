import React from 'react';
import './home.styles.scss';

export default ({ ctrl }) => (
    // ##################################################

    <app-home>
        <span className="page-title">{ctrl.pageTitle}</span>
    </app-home>

    // ##################################################
);
