import React from 'react';
import './not-found.styles.css';

export default ({ ctrl }) => (
    // ##################################################

    <div className="block">
        <h1>{ctrl.pageTitle}</h1>
    </div>

    // ##################################################
);
