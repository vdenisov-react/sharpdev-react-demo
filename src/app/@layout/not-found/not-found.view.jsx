import React from 'react';
import './not-found.styles.scss';

export default ({ ctrl }) => (
    // ##################################################

    <div className='block'>
        <h1>{ctrl.pageTitle}</h1>
    </div>

    // ##################################################
);
