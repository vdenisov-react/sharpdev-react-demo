import React from 'react';
import './not-found.styles.scss';

export default ({ ctrl }) => (
    // ##################################################

    <app-not-found>
        <div className='container'>
            <h1>{ctrl.pageTitle}</h1>
        </div>
    </app-not-found>

    // ##################################################
);
